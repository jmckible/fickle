%p= link_to 'Back', [:edit, @poll]

%h1= @poll.name
%h2= @choice.name

= form @choice do |f|
  = f.text_field :name
  = f.check_box :live
  = f.submit 'Update'


%p or

= link_to 'Delete', @choice, method: :delete, data: { confirm: 'Are you sure you want to delete this choice?' }
